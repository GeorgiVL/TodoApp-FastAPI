# Copy to ".env" to point the frontend at a non-default API location.
# Defaults to http://localhost:8000 when unset.
# For Docker: leave empty ("") so the Nginx proxy handles API requests on the same origin.
VITE_API_URL=http://localhost:8000
